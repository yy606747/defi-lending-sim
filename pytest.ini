[pytest]
testpaths = tests
pythonpath = backend
addopts = -ra
markers =
    financial: DeFi money, precision, risk, and invariant tests
    integration: API and cross-module workflow tests
    regression: Tests that lock known or likely defect patterns

